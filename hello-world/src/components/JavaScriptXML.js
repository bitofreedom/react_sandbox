import React from 'react'

/**
 * @description JSX - JavaScript XML (Extension to JavaScript language syntax)
 * Enables writing XML-like code for elements and components
 * JSX tags have a tag name, attributes, and children
 * JSX is not a necessity to write React applications
 * JSX makes React code simpler and elegant
 * Ultimately 'transpiles' to JavaScript which is understood by browsers
 */

// ES6 (arrow function) approach
// const JSXML = () => <h1>Hello Darrin!</h1>

export default JSXML